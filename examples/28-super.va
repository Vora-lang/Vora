print("=== 1. Basic super call ===")
class Animal(name) {
    this.name = name
    func speak() {
        return "..."
    }
    func describe() {
        return "I am " + this.name
    }
}
class Dog : Animal(name, breed) {
    this.breed = breed
    func speak() {
        return super.speak() + " woof!"
    }
    func describe() {
        return super.describe() + " the " + this.breed
    }
}
let d = Dog("Rex", "Husky")
print(d.speak())
print(d.describe())

print("\n=== 2. Multi-level super chain ===")
class A() {
    func chain() { return "A" }
}
class B : A() {
    func chain() { return super.chain() + " → B" }
}
class C : B() {
    func chain() { return super.chain() + " → C" }
}
let c = C()
print("chain:", c.chain())

print("\n=== 3. Super in constructor ===")
class Shape(color) {
    this.color = color
    this.type = "shape"
}
class Circle : Shape(color, radius) {
    this.radius = radius
    func info() {
        return "A " + this.color + " circle, r=" + this.radius
    }
}
let circle = Circle("red", 5)
print(circle.info())
print("type:", circle.type)

print("\n=== 4. Super with multiple inheritance ===")
class X() {
    func msg() { return "X" }
}
class Y : X() {
    func msg() { return super.msg() + " → Y" }
}
class Z : X() {
    func msg() { return "Z" }
}
class W : Y, Z() {
    func msg() { return super.msg() + " → W" }
}
let w = W()
print("W.msg:", w.msg())

print("\n=== 5. Only call super when you need parent behavior ===")
class Logger() {
    func log(msg) {
        print("[LOG]", msg)
    }
}
class TimestampLogger : Logger() {
    func log(msg) {
        super.log(msg)
        print("[TIME]", clock())
    }
}
let tl = TimestampLogger()
tl.log("test message")
