// tests/interpreter/test_module_caching.va — Test module caching behavior
import "math"
import "math"     // second import should hit cache

assert(math.PI > 3.14 && math.PI < 3.15, "cached math.PI")
assert(math.sqrt(16) == 4.0, "cached math.sqrt")

print("test_module_caching: PASS")
